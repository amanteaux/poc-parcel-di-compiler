{
  "name": "poc-parcel-di-compiler",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@wessberg/di": "^2.0.3"
  },
  "devDependencies": {
    "@wessberg/di-compiler": "^2.1.1",
    "babel-types": "^6.26.0",
    "parcel-bundler": "^1.12.4",
    "parcel-plugin-ttypescript": "^1.0.2",
    "ts-node": ">=8.0.2",
    "ttypescript": "^1.5.12",
    "typescript": ">=3.2.2"
  },
  "scripts": {
    "start": "parcel src/index.html -d build --open",
    "prebuild": "rm -rf build",
    "build": "parcel build src/index.html -d build"
  }
}
